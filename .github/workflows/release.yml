name: Publish

on:
  push:
    tags:
      - v*

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: '12.x'
      - name: npm install
        run: npm install
        env:
          NPM_AUTH_TOKEN: ${{ secrets.NPM_AUTH_TOKEN }}
      - name: Cache node modules
        uses: actions/cache@v2
        env:
          cache-name: cache-node-modules
        with:
          # npm cache files are stored in `~/.npm` on Linux/macOS
          path: ~/.npm
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
    
      - name: NPM Build Install Lerna
        shell: bash
        run: npm install -g lerna
      - name: NPM Build Install TypeScript 
        shell: bash
        run: npm install -g typescript tsdoc
      
      
      - name: Get the version
        id: get_version
        run: echo ::set-output name=VERSION::$(git describe --abbrev=0 --tags)
   
      - name: Build Lerna bootstrap
        shell: bash
        run: |
          lerna bootstrap
          lerna ls --graph
          make version 
          make publish
      # - name: Test
      #   shell: bash
      #   run: |
      #     lerna run test
      # - name: Increment dev Version
      #   shell: bash
      #   run: |
      #     lerna run releaseDev
      # - name: Publish API Gateway
      #   shell: bash
      #   run: |
      #     cd packages/api-gateway-custom-authorizer/
      #     npm publish
      # - name: Publish Notify Client
      #   shell: bash
      #   run: |
      #     cd packages/itops-notify/
      #     npm publish
      # - name: Publish HTTPS Client
      #   shell: bash
      #   run: |
      #     cd packages/itops-https/
      #     npm publish
